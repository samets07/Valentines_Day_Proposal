<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Biz ❤️</title>

<style>
:root{
  --bg1:#ff9a9e;
  --bg2:#fad0c4;
  --red:#e63946;
}

*{box-sizing:border-box}
html,body{
  margin:0; padding:0; height:100%;
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Arial, sans-serif;
  background: linear-gradient(120deg,var(--bg1),var(--bg2));
  color:#fff;
  overflow:hidden;
}

/* AÇILIŞ */
#intro{
  position:fixed; inset:0;
  display:flex; flex-direction:column; align-items:center; justify-content:center;
  gap:14px;
  background: linear-gradient(120deg,var(--bg1),var(--bg2));
  z-index:10;
  transition: opacity 1.2s ease, transform 1.2s ease;
}

.heart{
  width:160px; height:160px;
  position:relative;
  transform: rotate(-45deg);
  background:var(--red);
  animation: beat 1.2s infinite ease-in-out;
  cursor:pointer;
  box-shadow:0 20px 40px rgba(0,0,0,.25);
}
.heart:before,
.heart:after{
  content:"";
  position:absolute;
  width:160px; height:160px;
  background:var(--red);
  border-radius:50%;
}
.heart:before{ top:-80px; left:0; }
.heart:after{ left:80px; top:0; }

@keyframes beat{
  0%{ transform:rotate(-45deg) scale(1); }
  25%{ transform:rotate(-45deg) scale(1.08); }
  40%{ transform:rotate(-45deg) scale(1); }
  60%{ transform:rotate(-45deg) scale(1.12); }
  100%{ transform:rotate(-45deg) scale(1); }
}

#intro small{ opacity:.9 }
#intro .hint{ font-size:14px; opacity:.85 }

/* İÇERİK */
#content{
  position:relative;
  height:100%;
  overflow-y:auto;
  padding:60px 16px 80px;
  opacity:0;
}

.section{
  max-width:720px;
  margin:0 auto 40px;
  text-align:center;
  animation: reveal 1.2s ease forwards;
}

.section.delay-1{ animation-delay:.2s }
.section.delay-2{ animation-delay:.5s }
.section.delay-3{ animation-delay:.8s }

h1{
  font-size:34px;
  margin-bottom:12px;
}
p{
  font-size:18px;
  line-height:1.7;
}

.gallery img{
  width:100%;
  max-width:420px;
  border-radius:18px;
  margin:16px auto;
  display:block;
  box-shadow:0 18px 36px rgba(0,0,0,.25);
  animation: photoIn 1.1s ease forwards;
}

@keyframes reveal{
  from{ opacity:0; transform:translateY(30px); }
  to{ opacity:1; transform:translateY(0); }
}
@keyframes photoIn{
  from{ opacity:0; transform:scale(.95); }
  to{ opacity:1; transform:scale(1); }
}

/* MÜZİK */
.music-btn{
  position:fixed; right:14px; bottom:14px;
  background:rgba(0,0,0,.35);
  backdrop-filter: blur(6px);
  color:#fff; border:none;
  padding:12px 16px;
  border-radius:999px;
  font-size:14px;
}
</style>
</head>

<body>

<!-- AÇILIŞ -->
<div id="intro">
  <div class="heart" id="heart"></div>
  <small>Kalbime dokun…</small>
  <div class="hint">💖</div>
</div>

<!-- İÇERİK -->
<div id="content">
  <div class="section delay-1">
    <h1>Biz ❤️</h1>
    <p>
      Seni sevmek, her gün yeniden öğrenilen bir mucize gibi.
      Yanımdayken zaman yavaşlıyor, dünya daha sessiz oluyor.
    </p>
  </div>

  <div class="section delay-2 gallery">
    <img src="foto1.jpg" alt="Birlikte">
    <p>
      Bu an… Kalbimin ritmini değiştiren an.
    </p>
    <img src="foto2.jpg" alt="Birlikte">
    <p>
      Gülüşün, en sevdiğim manzara.
    </p>
  </div>

  <div class="section delay-3">
    <p>
      14 Şubat’ta ve diğer bütün günlerde,
      kalbim hep sana doğru atıyor.
    </p>
  </div>
</div>

<button class="music-btn" id="musicBtn">🎶 Müziği Başlat</button>

<audio id="music">
  <source src="muzik.mp3" type="audio/mpeg">
</audio>

<script>
const intro = document.getElementById('intro');
const content = document.getElementById('content');
const heart = document.getElementById('heart');
const music = document.getElementById('music');
const musicBtn = document.getElementById('musicBtn');

heart.addEventListener('click', () => {
  // Kalp kaybolurken
  intro.style.opacity = '0';
  intro.style.transform = 'scale(0.95)';
  setTimeout(()=>{
    intro.style.display='none';
    document.body.style.overflow='auto';
    content.style.opacity='1';
  }, 1200);
});

musicBtn.addEventListener('click', ()=>{
  music.play();
});
</script>

</body>
</html>
